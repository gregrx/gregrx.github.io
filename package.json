{
  "name": "ddlearn",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "concurrently \"npm run sync\" \"npm run tailwind\"",
    "sync": "browser-sync start --watch --files=\"./**/*.html,./**/*.css,./**/*.js,.\" -s",
    "tailwind": "npx tailwindcss -i ./css/style.css -o ./css/outofstyle.css --watch"
  },
  "keywords": [],
  "main": "main.js",
  "license": "MIT",
  "dependencies": {
    "alpinejs": "^3.13.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  }
}
